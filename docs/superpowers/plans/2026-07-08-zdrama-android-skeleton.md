# ZdramaAndroid Skeleton Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a standalone, AGP 4.2.2 / Gradle 6.8.3 Android app skeleton in `ZdramaAndroid/` for the future Agnes mobile drama MVP.

**Architecture:** `ZdramaAndroid/` is an independent single-module Android app. The first milestone only establishes the Gradle build, XML/ViewBinding Activity shell, Android resources, and minimal Kotlin package boundaries for data, remote API, repository, domain, skill, worker, and UI layers.

**Tech Stack:** Android Gradle Plugin 4.2.2, Gradle Wrapper 6.8.3, Kotlin 1.5.31, XML + ViewBinding, AppCompat, Material Components, Room, Retrofit/OkHttp, WorkManager, AndroidX Media2.

## Global Constraints

- Android Gradle Plugin: `4.2.2`.
- Gradle Wrapper: `6.8.3`.
- Kotlin Gradle plugin: `1.5.31`.
- UI: Android XML + ViewBinding; do not add Jetpack Compose.
- Project directory: `ZdramaAndroid/`.
- Package name: `com.huobao.zdrama`.
- `compileSdkVersion 33`, `minSdkVersion 23`, `targetSdkVersion 33`.
- Use Java 8 source/target compatibility.
- Do not implement the complete Agnes text/image/video generation flow in this milestone.
- Do not depend on the existing Node backend or frontend.
- Do not ignore `gradle/wrapper/gradle-wrapper.jar`; the wrapper must be committed for reproducible Gradle 6.8.3 execution.

---

## File Structure

Create or modify these files:

- Modify: `.gitignore` — add Android/Gradle generated outputs while preserving wrapper jar tracking.
- Create: `ZdramaAndroid/settings.gradle` — independent Gradle project settings.
- Create: `ZdramaAndroid/build.gradle` — root buildscript with AGP 4.2.2 and Kotlin 1.5.31.
- Create: `ZdramaAndroid/gradle.properties` — AndroidX and JVM build flags.
- Create: `ZdramaAndroid/gradlew` — Unix Gradle wrapper script.
- Create: `ZdramaAndroid/gradlew.bat` — Windows Gradle wrapper script.
- Create: `ZdramaAndroid/gradle/wrapper/gradle-wrapper.properties` — Gradle 6.8.3 distribution URL.
- Create: `ZdramaAndroid/gradle/wrapper/gradle-wrapper.jar` — generated by Gradle wrapper command if available; otherwise copied from a trusted local Gradle wrapper installation.
- Create: `ZdramaAndroid/app/build.gradle` — Android app module configuration and dependencies, using target SDK 33 and Media2.
- Create: `ZdramaAndroid/app/proguard-rules.pro` — release shrinker placeholder rules.
- Create: `ZdramaAndroid/app/src/main/AndroidManifest.xml` — app manifest and `MainActivity` declaration.
- Create: `ZdramaAndroid/app/src/main/res/values/colors.xml` — base colors.
- Create: `ZdramaAndroid/app/src/main/res/values/strings.xml` — app strings.
- Create: `ZdramaAndroid/app/src/main/res/values/styles.xml` — Material/AppCompat theme.
- Create: `ZdramaAndroid/app/src/main/res/layout/activity_main.xml` — XML skeleton UI.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/ui/main/MainActivity.kt` — ViewBinding Activity.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/ProjectEntity.kt` — Room project entity.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/CharacterEntity.kt` — Room character entity.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/SceneEntity.kt` — Room scene entity.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/StoryboardEntity.kt` — Room storyboard entity.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/SkillConfigEntity.kt` — Room skill config entity.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/DramaDao.kt` — minimal DAO signatures.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/DramaDatabase.kt` — Room database.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/remote/AgnesApiService.kt` — Retrofit API service and DTOs.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/remote/AgnesAuthInterceptor.kt` — token injection boundary.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/repository/DramaRepository.kt` — repository boundary.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/repository/GenerationRepository.kt` — generation boundary.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/repository/SkillRepository.kt` — skill config boundary.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/domain/model/DramaModels.kt` — domain enums and models.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/domain/usecase/CreateDramaUseCase.kt` — create project use case boundary.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/domain/usecase/GenerateImagesUseCase.kt` — image use case boundary.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/domain/usecase/GenerateVideosUseCase.kt` — video use case boundary.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/domain/usecase/RetryStoryboardUseCase.kt` — retry use case boundary.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/skill/BuiltInSkillRegistry.kt` — fixed built-in skill list.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/skill/SkillEngine.kt` — skill execution boundary.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/skill/SkillInputValidator.kt` — validation boundary.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/skill/SkillJsonParser.kt` — JSON parsing boundary.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/worker/GenerationWorker.kt` — WorkManager placeholder worker.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/ui/settings/SettingsPlaceholder.kt` — settings UI boundary marker.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/ui/create/CreatePlaceholder.kt` — create UI boundary marker.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/ui/project/ProjectPlaceholder.kt` — project UI boundary marker.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/ui/storyboard/StoryboardPlaceholder.kt` — storyboard UI boundary marker.
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/ui/player/PlayerPlaceholder.kt` — player UI boundary marker.

---

### Task 1: Gradle Project Shell

**Files:**
- Modify: `.gitignore`
- Create: `ZdramaAndroid/settings.gradle`
- Create: `ZdramaAndroid/build.gradle`
- Create: `ZdramaAndroid/gradle.properties`
- Create: `ZdramaAndroid/gradlew`
- Create: `ZdramaAndroid/gradlew.bat`
- Create: `ZdramaAndroid/gradle/wrapper/gradle-wrapper.properties`
- Create: `ZdramaAndroid/gradle/wrapper/gradle-wrapper.jar`

**Interfaces:**
- Consumes: empty `ZdramaAndroid/` directory.
- Produces: independent Gradle project named `ZdramaAndroid` with included `:app` module; later tasks rely on `com.android.application`, Kotlin plugin, and Gradle wrapper paths.

- [ ] **Step 1: Append Android ignore rules**

Add these lines to the repository root `.gitignore` if they are not already present:

```gitignore

# Android / Gradle
.gradle/
build/
*/build/
local.properties
captures/
.externalNativeBuild/
.cxx/
*.apk
*.ap_
*.aab
*.iml
```

Do not add `gradle-wrapper.jar` to `.gitignore`.

- [ ] **Step 2: Create Gradle settings**

Create `ZdramaAndroid/settings.gradle`:

```groovy
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'ZdramaAndroid'
include ':app'
```

If Gradle 6.8.3 rejects `dependencyResolutionManagement`, replace the file with this compatible version:

```groovy
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'ZdramaAndroid'
include ':app'
```

and keep repositories in root `build.gradle` as shown in Step 3.

- [ ] **Step 3: Create root build file**

Create `ZdramaAndroid/build.gradle`:

```groovy
buildscript {
    ext.kotlin_version = '1.5.31'

    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:4.2.2'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
```

- [ ] **Step 4: Create Gradle properties**

Create `ZdramaAndroid/gradle.properties`:

```properties
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
kotlin.code.style=official
```

- [ ] **Step 5: Generate or install Gradle 6.8.3 wrapper**

Preferred command if a local `gradle` command is installed:

```bash
cd ZdramaAndroid
gradle wrapper --gradle-version 6.8.3 --distribution-type bin
```

Expected: creates `gradlew`, `gradlew.bat`, `gradle/wrapper/gradle-wrapper.properties`, and `gradle/wrapper/gradle-wrapper.jar`.

If `gradle` is not installed, copy the standard Gradle wrapper scripts and wrapper jar from an existing trusted Gradle installation or Android Studio generated project, then ensure `gradle-wrapper.properties` contains exactly:

```properties
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-6.8.3-all.zip
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
```

- [ ] **Step 6: Make Unix wrapper executable**

Run:

```bash
chmod +x ZdramaAndroid/gradlew
```

Expected: `ZdramaAndroid/gradlew` is executable.

- [ ] **Step 7: Verify wrapper metadata**

Run:

```bash
rg -n "6.8.3|4.2.2|1.5.31" ZdramaAndroid
```

Expected output includes:

```text
ZdramaAndroid/build.gradle:...4.2.2
ZdramaAndroid/build.gradle:...1.5.31
ZdramaAndroid/gradle/wrapper/gradle-wrapper.properties:...gradle-6.8.3-all.zip
```

- [ ] **Step 8: Record status**

Run:

```bash
git status --short
```

Expected: only the intended Gradle shell files and approved docs are changed. Do not commit unless the user explicitly asks.

---

### Task 2: Android App Module And Resources

**Files:**
- Create: `ZdramaAndroid/app/build.gradle`
- Create: `ZdramaAndroid/app/proguard-rules.pro`
- Create: `ZdramaAndroid/app/src/main/AndroidManifest.xml`
- Create: `ZdramaAndroid/app/src/main/res/values/colors.xml`
- Create: `ZdramaAndroid/app/src/main/res/values/strings.xml`
- Create: `ZdramaAndroid/app/src/main/res/values/styles.xml`
- Create: `ZdramaAndroid/app/src/main/res/layout/activity_main.xml`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/ui/main/MainActivity.kt`

**Interfaces:**
- Consumes: `:app` module name from Task 1.
- Produces: Android app module with package `com.huobao.zdrama`, ViewBinding class `ActivityMainBinding`, and launchable `MainActivity`.

- [ ] **Step 1: Create app module build file**

Create `ZdramaAndroid/app/build.gradle`:

```groovy
plugins {
    id 'com.android.application'
    id 'kotlin-android'
}

android {
    compileSdkVersion 33

    defaultConfig {
        applicationId "com.huobao.zdrama"
        minSdkVersion 23
        targetSdkVersion 33
        versionCode 1
        versionName "0.1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildFeatures {
        viewBinding true
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"

    implementation 'androidx.core:core-ktx:1.6.0'
    implementation 'androidx.appcompat:appcompat:1.3.1'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    implementation 'com.google.android.material:material:1.4.0'

    implementation 'androidx.lifecycle:lifecycle-viewmodel-ktx:2.3.1'
    implementation 'androidx.lifecycle:lifecycle-livedata-ktx:2.3.1'

    implementation 'androidx.room:room-runtime:2.3.0'

    implementation 'com.squareup.retrofit2:retrofit:2.9.0'
    implementation 'com.squareup.retrofit2:converter-gson:2.9.0'
    implementation 'com.squareup.okhttp3:okhttp:4.9.3'
    implementation 'com.squareup.okhttp3:logging-interceptor:4.9.3'
    implementation 'com.google.code.gson:gson:2.8.9'

    implementation 'androidx.work:work-runtime-ktx:2.6.0'

    implementation 'androidx.media2:media2-player:1.2.1'
    implementation 'androidx.media2:media2-widget:1.2.1'

    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.1.3'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.4.0'
}
```

- [ ] **Step 2: Create ProGuard rules file**

Create `ZdramaAndroid/app/proguard-rules.pro`:

```proguard
# Skeleton project: feature-specific shrinker rules will be added with the first release build.
```

- [ ] **Step 3: Create Android manifest**

Create `ZdramaAndroid/app/src/main/AndroidManifest.xml`:

```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZdramaAndroid">
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
```

If no launcher icons are generated in this skeleton, change `android:icon` and `android:roundIcon` to `@drawable/ic_launcher_foreground` only after adding that drawable. The preferred path is to create vector launcher placeholders in Task 2 Step 4.

- [ ] **Step 4: Create resource values and launcher vectors**

Create `ZdramaAndroid/app/src/main/res/values/colors.xml`:

```xml
<resources>
    <color name="zdrama_black">#101114</color>
    <color name="zdrama_surface">#1A1C20</color>
    <color name="zdrama_primary">#2F80ED</color>
    <color name="zdrama_on_primary">#FFFFFF</color>
    <color name="zdrama_text_primary">#F5F7FA</color>
    <color name="zdrama_text_secondary">#B8C0CC</color>
</resources>
```

Create `ZdramaAndroid/app/src/main/res/values/strings.xml`:

```xml
<resources>
    <string name="app_name">ZDrama Android</string>
    <string name="main_title">ZDrama Android</string>
    <string name="main_subtitle">Android Agnes drama skeleton</string>
    <string name="settings_placeholder">Settings</string>
    <string name="create_placeholder">Create</string>
    <string name="projects_placeholder">Projects</string>
</resources>
```

Create `ZdramaAndroid/app/src/main/res/values/styles.xml`:

```xml
<resources>
    <style name="Theme.ZdramaAndroid" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/zdrama_primary</item>
        <item name="colorPrimaryVariant">@color/zdrama_primary</item>
        <item name="colorOnPrimary">@color/zdrama_on_primary</item>
        <item name="android:windowBackground">@color/zdrama_black</item>
        <item name="android:fontFamily">sans</item>
    </style>
</resources>
```

Create `ZdramaAndroid/app/src/main/res/drawable/ic_launcher_foreground.xml`:

```xml
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#2F80ED"
        android:pathData="M16,16h76v76h-76z" />
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M32,30h30c10,0 18,8 18,18v12c0,10 -8,18 -18,18h-30zM44,42v24h16c4,0 8,-4 8,-8v-8c0,-4 -4,-8 -8,-8z" />
</vector>
```

Create `ZdramaAndroid/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml`:

```xml
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/zdrama_black" />
    <foreground android:drawable="@drawable/ic_launcher_foreground" />
</adaptive-icon>
```

Create `ZdramaAndroid/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml`:

```xml
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/zdrama_black" />
    <foreground android:drawable="@drawable/ic_launcher_foreground" />
</adaptive-icon>
```

- [ ] **Step 5: Create main layout**

Create `ZdramaAndroid/app/src/main/res/layout/activity_main.xml`:

```xml
<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/zdrama_black"
    android:padding="24dp">

    <TextView
        android:id="@+id/titleText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/main_title"
        android:textColor="@color/zdrama_text_primary"
        android:textSize="28sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/subtitleText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/main_subtitle"
        android:textColor="@color/zdrama_text_secondary"
        android:textSize="15sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/titleText" />

    <LinearLayout
        android:id="@+id/actionContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:orientation="vertical"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/subtitleText">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/settingsButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/settings_placeholder" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/createButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@string/create_placeholder" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/projectsButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@string/projects_placeholder" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
```

- [ ] **Step 6: Create MainActivity**

Create `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/ui/main/MainActivity.kt`:

```kotlin
package com.huobao.zdrama.ui.main

import android.os.Bundle
import android.widget.Toast
import androidx.appcompat.app.AppCompatActivity
import com.huobao.zdrama.databinding.ActivityMainBinding

class MainActivity : AppCompatActivity() {
    private lateinit var binding: ActivityMainBinding

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ActivityMainBinding.inflate(layoutInflater)
        setContentView(binding.root)

        binding.settingsButton.setOnClickListener { showPlaceholder("Settings") }
        binding.createButton.setOnClickListener { showPlaceholder("Create") }
        binding.projectsButton.setOnClickListener { showPlaceholder("Projects") }
    }

    private fun showPlaceholder(name: String) {
        Toast.makeText(this, "$name screen will be implemented in the MVP phase", Toast.LENGTH_SHORT).show()
    }
}
```

- [ ] **Step 7: Run resource/build verification**

Run:

```bash
cd ZdramaAndroid
./gradlew :app:assembleDebug
```

Expected if environment is ready: `BUILD SUCCESSFUL`.

Acceptable environment failures to report exactly: Android SDK missing, SDK 33 missing, Gradle distribution download blocked, Maven dependency download blocked.

- [ ] **Step 8: Record status**

Run:

```bash
git status --short
```

Expected: only the intended app shell files and approved earlier files are changed. Do not commit unless the user explicitly asks.

---

### Task 3: Room Data Skeleton

**Files:**
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/ProjectEntity.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/CharacterEntity.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/SceneEntity.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/StoryboardEntity.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/SkillConfigEntity.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/DramaDao.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/local/DramaDatabase.kt`

**Interfaces:**
- Consumes: Room dependency from Task 2.
- Produces: `DramaDatabase`, `DramaDao`, and entity classes used by repository skeletons in Task 5.

- [ ] **Step 1: Create ProjectEntity**

Create `ProjectEntity.kt`:

```kotlin
package com.huobao.zdrama.data.local

import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity(tableName = "projects")
data class ProjectEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0L,
    val title: String,
    val prompt: String,
    val style: String,
    val targetAudience: String,
    val aspectRatio: String,
    val shotCount: Int,
    val shotDurationSeconds: Int,
    val status: String,
    val currentStage: String,
    val errorMessage: String?,
    val createdAt: Long,
    val updatedAt: Long
)
```

- [ ] **Step 2: Create CharacterEntity**

Create `CharacterEntity.kt`:

```kotlin
package com.huobao.zdrama.data.local

import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity(tableName = "characters")
data class CharacterEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0L,
    val projectId: Long,
    val name: String,
    val role: String,
    val personality: String,
    val description: String,
    val appearance: String,
    val visualPrompt: String
)
```

- [ ] **Step 3: Create SceneEntity**

Create `SceneEntity.kt`:

```kotlin
package com.huobao.zdrama.data.local

import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity(tableName = "scenes")
data class SceneEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0L,
    val projectId: Long,
    val name: String,
    val description: String,
    val atmosphere: String,
    val visualPrompt: String
)
```

- [ ] **Step 4: Create StoryboardEntity**

Create `StoryboardEntity.kt`:

```kotlin
package com.huobao.zdrama.data.local

import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity(tableName = "storyboards")
data class StoryboardEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0L,
    val projectId: Long,
    val index: Int,
    val title: String,
    val description: String,
    val sceneName: String,
    val charactersJson: String,
    val imagePrompt: String,
    val videoPrompt: String,
    val durationSeconds: Int,
    val imageStatus: String,
    val imageTaskId: String?,
    val imageUrl: String?,
    val localImagePath: String?,
    val videoStatus: String,
    val videoTaskId: String?,
    val videoUrl: String?,
    val localVideoPath: String?,
    val errorMessage: String?
)
```

- [ ] **Step 5: Create SkillConfigEntity**

Create `SkillConfigEntity.kt`:

```kotlin
package com.huobao.zdrama.data.local

import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity(tableName = "skill_configs")
data class SkillConfigEntity(
    @PrimaryKey val id: String,
    val name: String,
    val version: String,
    val required: Boolean,
    val enabled: Boolean,
    val modelType: String,
    val temperature: Double,
    val maxOutputTokens: Int,
    val outputMode: String,
    val promptTemplate: String
)
```

- [ ] **Step 6: Create DAO**

Create `DramaDao.kt`:

```kotlin
package com.huobao.zdrama.data.local

import androidx.room.Dao
import androidx.room.Insert
import androidx.room.OnConflictStrategy
import androidx.room.Query

@Dao
interface DramaDao {
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertProject(project: ProjectEntity): Long

    @Query("SELECT * FROM projects ORDER BY updatedAt DESC")
    suspend fun getProjects(): List<ProjectEntity>

    @Query("SELECT * FROM storyboards WHERE projectId = :projectId ORDER BY `index` ASC")
    suspend fun getStoryboards(projectId: Long): List<StoryboardEntity>
}
```

- [ ] **Step 7: Create Room database**

Create `DramaDatabase.kt`:

```kotlin
package com.huobao.zdrama.data.local

import androidx.room.Database
import androidx.room.RoomDatabase

@Database(
    entities = [
        ProjectEntity::class,
        CharacterEntity::class,
        SceneEntity::class,
        StoryboardEntity::class,
        SkillConfigEntity::class
    ],
    version = 1,
    exportSchema = false
)
abstract class DramaDatabase : RoomDatabase() {
    abstract fun dramaDao(): DramaDao
}
```

- [ ] **Step 8: Build to verify Room runtime skeleton**

Run:

```bash
cd ZdramaAndroid
./gradlew :app:assembleDebug
```

Expected: `BUILD SUCCESSFUL`. This skeleton intentionally verifies Room annotations with `room-runtime` only; Room compiler/KAPT is deferred because Room 2.3.0 compiler fails on Mac arm64 native sqlite, while newer Room compiler artifacts require a newer Kotlin metadata level than Kotlin 1.5.31.

- [ ] **Step 9: Record status**

Run:

```bash
git status --short
```

Expected: only the intended Room skeleton files and approved earlier files are changed. Do not commit unless the user explicitly asks.

---

### Task 4: Remote Agnes API Skeleton

**Files:**
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/remote/AgnesApiService.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/remote/AgnesAuthInterceptor.kt`

**Interfaces:**
- Consumes: Retrofit, Gson converter, OkHttp dependencies from Task 2.
- Produces: `AgnesApiService`, request/response DTOs, `ApiKeyProvider`, and `AgnesAuthInterceptor` used by future network client wiring.

- [ ] **Step 1: Create Agnes API service and DTOs**

Create `AgnesApiService.kt`:

```kotlin
package com.huobao.zdrama.data.remote

import retrofit2.http.Body
import retrofit2.http.POST

interface AgnesApiService {
    @POST("v1/chat/completions")
    suspend fun createChatCompletion(@Body request: ChatCompletionRequest): ChatCompletionResponse
}

data class ChatCompletionRequest(
    val model: String,
    val messages: List<ChatMessage>,
    val temperature: Double,
    val max_tokens: Int
)

data class ChatMessage(
    val role: String,
    val content: String
)

data class ChatCompletionResponse(
    val id: String?,
    val choices: List<ChatChoice>
)

data class ChatChoice(
    val index: Int,
    val message: ChatMessage
)

data class ImageGenerationRequest(
    val model: String,
    val prompt: String,
    val aspectRatio: String
)

data class ImageGenerationResponse(
    val taskId: String?,
    val imageUrl: String?,
    val base64: String?
)

data class VideoGenerationRequest(
    val model: String,
    val prompt: String,
    val imageUrl: String?,
    val durationSeconds: Int
)

data class VideoGenerationResponse(
    val taskId: String?,
    val videoUrl: String?
)
```

- [ ] **Step 2: Create auth interceptor**

Create `AgnesAuthInterceptor.kt`:

```kotlin
package com.huobao.zdrama.data.remote

import okhttp3.Interceptor
import okhttp3.Response

interface ApiKeyProvider {
    fun getApiKey(): String?
}

class AgnesAuthInterceptor(
    private val apiKeyProvider: ApiKeyProvider
) : Interceptor {
    override fun intercept(chain: Interceptor.Chain): Response {
        val originalRequest = chain.request()
        val apiKey = apiKeyProvider.getApiKey()

        val requestBuilder = originalRequest.newBuilder()
        if (!apiKey.isNullOrBlank()) {
            requestBuilder.header("Authorization", "Bearer $apiKey")
        }

        return chain.proceed(requestBuilder.build())
    }
}
```

- [ ] **Step 3: Build to verify Retrofit/OkHttp skeleton**

Run:

```bash
cd ZdramaAndroid
./gradlew :app:assembleDebug
```

Expected: `BUILD SUCCESSFUL`.

- [ ] **Step 4: Record status**

Run:

```bash
git status --short
```

Expected: only the intended Agnes API skeleton files and approved earlier files are changed. Do not commit unless the user explicitly asks.

---

### Task 5: Domain And Repository Boundaries

**Files:**
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/repository/DramaRepository.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/repository/GenerationRepository.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/data/repository/SkillRepository.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/domain/model/DramaModels.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/domain/usecase/CreateDramaUseCase.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/domain/usecase/GenerateImagesUseCase.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/domain/usecase/GenerateVideosUseCase.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/domain/usecase/RetryStoryboardUseCase.kt`

**Interfaces:**
- Consumes: `DramaDao`, `ProjectEntity`, `StoryboardEntity` from Task 3.
- Produces: repository classes and use case classes used by future ViewModels/workers.

- [ ] **Step 1: Create domain models**

Create `DramaModels.kt`:

```kotlin
package com.huobao.zdrama.domain.model

enum class ProjectStatus {
    DRAFT,
    PROCESSING,
    COMPLETED,
    FAILED,
    CANCELLED
}

enum class GenerationStage {
    NONE,
    TEXT,
    IMAGE,
    VIDEO
}

enum class AssetStatus {
    PENDING,
    PROCESSING,
    COMPLETED,
    FAILED
}

data class CreateDramaInput(
    val title: String,
    val prompt: String,
    val style: String,
    val targetAudience: String,
    val aspectRatio: String,
    val shotCount: Int,
    val shotDurationSeconds: Int
)
```

- [ ] **Step 2: Create repositories**

Create `DramaRepository.kt`:

```kotlin
package com.huobao.zdrama.data.repository

import com.huobao.zdrama.data.local.DramaDao
import com.huobao.zdrama.data.local.ProjectEntity

class DramaRepository(
    private val dramaDao: DramaDao
) {
    suspend fun createProject(project: ProjectEntity): Long {
        return dramaDao.insertProject(project)
    }

    suspend fun getProjects(): List<ProjectEntity> {
        return dramaDao.getProjects()
    }
}
```

Create `GenerationRepository.kt`:

```kotlin
package com.huobao.zdrama.data.repository

import com.huobao.zdrama.data.local.DramaDao
import com.huobao.zdrama.data.local.StoryboardEntity

class GenerationRepository(
    private val dramaDao: DramaDao
) {
    suspend fun getStoryboards(projectId: Long): List<StoryboardEntity> {
        return dramaDao.getStoryboards(projectId)
    }
}
```

Create `SkillRepository.kt`:

```kotlin
package com.huobao.zdrama.data.repository

import com.huobao.zdrama.data.local.SkillConfigEntity

class SkillRepository {
    fun getDefaultSkillConfigs(): List<SkillConfigEntity> {
        return emptyList()
    }
}
```

- [ ] **Step 3: Create use cases**

Create `CreateDramaUseCase.kt`:

```kotlin
package com.huobao.zdrama.domain.usecase

import com.huobao.zdrama.data.local.ProjectEntity
import com.huobao.zdrama.data.repository.DramaRepository
import com.huobao.zdrama.domain.model.CreateDramaInput
import com.huobao.zdrama.domain.model.GenerationStage
import com.huobao.zdrama.domain.model.ProjectStatus

class CreateDramaUseCase(
    private val dramaRepository: DramaRepository
) {
    suspend fun execute(input: CreateDramaInput): Long {
        val now = System.currentTimeMillis()
        val project = ProjectEntity(
            title = input.title,
            prompt = input.prompt,
            style = input.style,
            targetAudience = input.targetAudience,
            aspectRatio = input.aspectRatio,
            shotCount = input.shotCount,
            shotDurationSeconds = input.shotDurationSeconds,
            status = ProjectStatus.DRAFT.name,
            currentStage = GenerationStage.NONE.name,
            errorMessage = null,
            createdAt = now,
            updatedAt = now
        )
        return dramaRepository.createProject(project)
    }
}
```

Create `GenerateImagesUseCase.kt`:

```kotlin
package com.huobao.zdrama.domain.usecase

class GenerateImagesUseCase {
    suspend fun execute(projectId: Long): Result<Unit> {
        return Result.success(Unit)
    }
}
```

Create `GenerateVideosUseCase.kt`:

```kotlin
package com.huobao.zdrama.domain.usecase

class GenerateVideosUseCase {
    suspend fun execute(projectId: Long): Result<Unit> {
        return Result.success(Unit)
    }
}
```

Create `RetryStoryboardUseCase.kt`:

```kotlin
package com.huobao.zdrama.domain.usecase

class RetryStoryboardUseCase {
    suspend fun execute(storyboardId: Long): Result<Unit> {
        return Result.success(Unit)
    }
}
```

- [ ] **Step 4: Build to verify domain/repository boundaries**

Run:

```bash
cd ZdramaAndroid
./gradlew :app:assembleDebug
```

Expected: `BUILD SUCCESSFUL`.

- [ ] **Step 5: Record status**

Run:

```bash
git status --short
```

Expected: only the intended domain/repository files and approved earlier files are changed. Do not commit unless the user explicitly asks.

---

### Task 6: Skill And Worker Skeletons

**Files:**
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/skill/BuiltInSkillRegistry.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/skill/SkillEngine.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/skill/SkillInputValidator.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/skill/SkillJsonParser.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/worker/GenerationWorker.kt`

**Interfaces:**
- Consumes: WorkManager dependency from Task 2.
- Produces: `BuiltInSkillRegistry`, `SkillEngine`, `SkillInputValidator`, `SkillJsonParser`, and `GenerationWorker` used by future MVP pipeline.

- [ ] **Step 1: Create built-in skill registry**

Create `BuiltInSkillRegistry.kt`:

```kotlin
package com.huobao.zdrama.skill

data class BuiltInSkill(
    val id: String,
    val name: String,
    val version: String,
    val required: Boolean,
    val enabledByDefault: Boolean,
    val outputMode: String
)

class BuiltInSkillRegistry {
    fun getSkills(): List<BuiltInSkill> {
        return listOf(
            BuiltInSkill("idea_expander", "Idea Expander", "1.0.0", true, true, "json_object"),
            BuiltInSkill("drama_structure_generator", "Drama Structure Generator", "1.0.0", true, true, "json_object"),
            BuiltInSkill("character_designer", "Character Designer", "1.0.0", true, true, "json_array"),
            BuiltInSkill("scene_designer", "Scene Designer", "1.0.0", true, true, "json_array"),
            BuiltInSkill("storyboard_breaker", "Storyboard Breaker", "1.0.0", true, true, "json_array")
        )
    }
}
```

- [ ] **Step 2: Create skill engine**

Create `SkillEngine.kt`:

```kotlin
package com.huobao.zdrama.skill

data class SkillExecutionRequest(
    val skillId: String,
    val inputJson: String
)

data class SkillExecutionResult(
    val skillId: String,
    val output: String
)

class SkillEngine(
    private val registry: BuiltInSkillRegistry,
    private val inputValidator: SkillInputValidator,
    private val jsonParser: SkillJsonParser
) {
    fun execute(request: SkillExecutionRequest): SkillExecutionResult {
        val skill = registry.getSkills().firstOrNull { it.id == request.skillId }
            ?: throw IllegalArgumentException("Unknown skill: ${request.skillId}")
        inputValidator.validate(request.inputJson)
        return SkillExecutionResult(skillId = skill.id, output = jsonParser.normalize(request.inputJson))
    }
}
```

- [ ] **Step 3: Create validator and parser**

Create `SkillInputValidator.kt`:

```kotlin
package com.huobao.zdrama.skill

class SkillInputValidator {
    fun validate(inputJson: String) {
        require(inputJson.isNotBlank()) { "Skill input must not be blank" }
    }
}
```

Create `SkillJsonParser.kt`:

```kotlin
package com.huobao.zdrama.skill

class SkillJsonParser {
    fun normalize(json: String): String {
        return json.trim()
    }
}
```

- [ ] **Step 4: Create WorkManager worker**

Create `GenerationWorker.kt`:

```kotlin
package com.huobao.zdrama.worker

import android.content.Context
import androidx.work.CoroutineWorker
import androidx.work.WorkerParameters

class GenerationWorker(
    appContext: Context,
    workerParams: WorkerParameters
) : CoroutineWorker(appContext, workerParams) {
    override suspend fun doWork(): Result {
        return Result.success()
    }
}
```

- [ ] **Step 5: Build to verify skill and worker skeletons**

Run:

```bash
cd ZdramaAndroid
./gradlew :app:assembleDebug
```

Expected: `BUILD SUCCESSFUL`.

- [ ] **Step 6: Record status**

Run:

```bash
git status --short
```

Expected: only the intended skill/worker files and approved earlier files are changed. Do not commit unless the user explicitly asks.

---

### Task 7: UI Boundary Placeholders And Final Verification

**Files:**
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/ui/settings/SettingsPlaceholder.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/ui/create/CreatePlaceholder.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/ui/project/ProjectPlaceholder.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/ui/storyboard/StoryboardPlaceholder.kt`
- Create: `ZdramaAndroid/app/src/main/java/com/huobao/zdrama/ui/player/PlayerPlaceholder.kt`

**Interfaces:**
- Consumes: package structure from Task 2.
- Produces: explicit package boundaries for future settings, create, project, storyboard, and player work.

- [ ] **Step 1: Create UI placeholder markers**

Create `SettingsPlaceholder.kt`:

```kotlin
package com.huobao.zdrama.ui.settings

object SettingsPlaceholder {
    const val routeName: String = "settings"
}
```

Create `CreatePlaceholder.kt`:

```kotlin
package com.huobao.zdrama.ui.create

object CreatePlaceholder {
    const val routeName: String = "create"
}
```

Create `ProjectPlaceholder.kt`:

```kotlin
package com.huobao.zdrama.ui.project

object ProjectPlaceholder {
    const val routeName: String = "project"
}
```

Create `StoryboardPlaceholder.kt`:

```kotlin
package com.huobao.zdrama.ui.storyboard

object StoryboardPlaceholder {
    const val routeName: String = "storyboard"
}
```

Create `PlayerPlaceholder.kt`:

```kotlin
package com.huobao.zdrama.ui.player

object PlayerPlaceholder {
    const val routeName: String = "player"
}
```

- [ ] **Step 2: Run final build**

Run:

```bash
cd ZdramaAndroid
./gradlew clean :app:assembleDebug
```

Expected if environment is ready:

```text
BUILD SUCCESSFUL
```

If build fails, classify exactly:

- Missing Android SDK or SDK 33: install SDK 33 or set `local.properties` with `sdk.dir` locally; do not commit `local.properties`.
- Gradle distribution download blocked: report the network/download failure.
- Dependency resolution failure: report the exact dependency and version; prefer downgrading that dependency without changing AGP 4.2.2 or Gradle 6.8.3.
- Kotlin/AGP incompatibility: report the exact compiler error and adjust Kotlin plugin only if necessary.

- [ ] **Step 3: Confirm Git status**

Run:

```bash
git status --short
```

Expected: only intended files are modified or added.

- [ ] **Step 4: Record status**

Run:

```bash
git status --short
```

Expected: only the intended UI boundary files and approved earlier files are changed. Do not commit unless the user explicitly asks.

---

## Self-Review

- Spec coverage: The plan covers independent `ZdramaAndroid/` Gradle project creation, AGP 4.2.2, Gradle 6.8.3, Kotlin 1.5.31, XML/ViewBinding, package boundaries, data skeleton, remote skeleton, repositories/use cases, skill/worker placeholders, `.gitignore`, and build verification.
- Intentional exclusions: It does not implement Agnes generation, API Key encrypted storage, full Room schema, real WorkManager pipeline, Media2 playback, FFmpeg, TTS, Compose, or Node backend integration, matching the spec non-goals.
- Placeholder scan: Placeholder classes are intentional package boundary markers with concrete code; no open-ended implementation instructions are left.
- Type consistency: `DramaDao`, `ProjectEntity`, `StoryboardEntity`, `DramaRepository`, and `CreateDramaUseCase` signatures align across tasks.
